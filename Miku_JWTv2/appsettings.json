{
  {
    // Configuración para el registro de eventos y mensajes en la aplicación.
    "Logging": {
      // Nivel de registro predeterminado para todos los componentes.
      "LogLevel": {
        "Default": "Information",
        // Nivel de registro para los mensajes relacionados con Microsoft.AspNetCore.
        "Microsoft.AspNetCore": "Warning"
      }
    },
    // Lista de hosts permitidos para la aplicación.
    "AllowedHosts": "*",
    // Cadena de conexión a la base de datos.
    "ConnectionStrings": {
      // Cadena de conexión para acceder a la base de datos MIKUDBV3 en SQL Server.
      "conexion": "Data Source=DESKTOP-PPN13H4\\SQLEXPRESS;Initial Catalog=MIKUDBV3; Integrated Security = true; Encrypt = False"
    },
    // Configuración para la generación y validación de tokens JWT.
    "Jwt": {
      // Clave secreta utilizada para firmar y verificar los tokens JWT.
      "Key": "ZLHT+ob[eoEti!87klort*[sqqlef47a[",
      // Emisor del token JWT (quién emite el token).
      "Issuer": "https://localhost:7075/",
      // Audiencia a la que está destinado el token JWT (dónde se puede usar).
      "Audience": "https://localhost:7075/",
      // Asunto del token JWT, generalmente utilizado para identificar el propósito o contexto.
      "Subject": "baseWebApiSubject"
    }
  }

